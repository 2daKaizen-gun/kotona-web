/**
 * 백엔드 없이 배포된 사이트인지.
 *
 * <p>명시적인 환경변수 하나로만 켠다. "백엔드에 닿지 않으면 자동으로 예시로 넘어간다"는
 * 설계도 검토했지만 채택하지 않았다 — 운영 중 백엔드가 잠깐 죽었을 때 조용히 가짜 데이터를
 * 진짜인 양 보여 주게 되고, 그건 이 기능이 피하려던 바로 그 상황이다.
 * 백엔드 주소가 설정돼 있는데 응답이 없으면 정직하게 오류를 낸다.
 *
 * <p>NEXT_PUBLIC_ 접두사가 없으므로 서버에서만 읽힌다. 서버 컴포넌트와
 * route handler 에서만 쓸 것.
 */
export const DEMO_MODE = process.env.KOTONA_DEMO === "true";
